# 하벳 리조트 사이트 수정 계획 및 이미지 교체 가이드

본 문서는 업체에서 전달한 수정 요청 사항을 분석하고, 현재 사이트 구조에 맞춘 실행 계획과 이미지 교체 가이드를 제공합니다.

## 1. 수정 요청 사항 분석 및 실행 계획

업체에서 전달한 수정 요청 사항은 크게 텍스트 수정, 이미지 교체, UI 개선으로 나눌 수 있습니다. 각 페이지별 주요 변경 사항과 실행 계획은 다음과 같습니다.

### 페이지별 주요 변경 사항

| 페이지 | 주요 변경 사항 | 실행 계획 |
|---|---|---|
| **초기 화면 (Index)** | 중정풀 사진 교체 (조약돌 없는 사진), 시그니처 메뉴 사진 변경 (치킨스테이크 → 한우스테이크) | `index.html` 이미지 경로 업데이트 |
| **ABOUT** | 텍스트 수정 ("이집트양식의 볼드함...", "소재와 질감" 삭제), 이미지 교체 (건축 & 디자인, 외부전경), 체크인/아웃 시간 수정 | `about.html` 텍스트 및 이미지 경로 수정 |
| **룸 (Rooms)** | 객실 소개글에 도면 보기 삽입, 객실사진 스크롤과 '예약하기' 버튼 겹침 현상 해결 | 각 객실 상세 페이지에 도면 추가, `style.css` 수정 |
| **레스토랑 (Restaurant)** | 메뉴 및 사진 전면 수정 (조식, 런치 & 디너, 테라스바, 푸드갤러리), 운영 안내 수정 (가격, 시간), 예약하기 버튼 링크 수정 | `restaurant.html` 텍스트 및 이미지 경로 전면 수정, 링크 타겟 확인 |
| **엔조이 (Enjoy)** | 베르데의 아침 사진 교체, 2번째 물결사진 변경 | `enjoy_*.html` 이미지 경로 업데이트 |
| **퍼실리티 (Facility)** | 수영장 텍스트 및 사진 수정 (중정풀, 수영장 전체, 개별수영장, 해수풀) | `facility.html` 텍스트 및 이미지 경로 수정 |

---

## 2. 이미지 교체 및 추가 가이드

현재 사이트의 이미지는 대부분 `http://gonylab7.speedgabia.com/havet/` 경로의 외부 서버(가비아)에 호스팅되어 있습니다. 이미지를 교체하거나 추가하려면 다음 단계를 따르야 합니다.

### 이미지 서버 업로드 절차

교체할 새 이미지(예: `하벳중정풀.jpg`, `0X5A0147.webp` 등)를 준비합니다. 웹 최적화를 위해 용량을 줄이고 적절한 포맷(JPG, WEBP)을 사용하는 것이 좋습니다. 가비아 서버(`gonylab7.speedgabia.com`)에 FTP/SFTP 클라이언트(예: FileZilla)를 사용하여 접속한 후, 기존 이미지가 있는 폴더 경로를 확인하고 새 이미지를 업로드합니다. 예를 들어, 중정풀 사진을 교체하려면 `/havet/special/1/` 폴더에 새 사진을 업로드하고 이름을 `1.jpg` 또는 `2.jpg`로 덮어쓰거나, 새 이름으로 업로드합니다.

### HTML/CSS 코드 수정 절차

서버에 이미지를 업로드한 후, 사이트 코드에서 해당 이미지 경로를 가리키도록 수정해야 합니다. 수정할 페이지의 HTML 파일(예: `index.html`, `about.html`)을 열고 기존 이미지 경로를 찾습니다. 새로 업로드한 이미지의 이름이나 경로로 코드를 수정한 후, 변경된 HTML 파일을 저장하고 GitHub에 Push하여 사이트에 반영합니다.

### 주요 이미지 경로 요약

| 카테고리 | 주요 경로 |
|---|---|
| **메인 슬라이드/배경** | `/havet/main/`, `/havet/thumb/main/` |
| **객실 사진** | `/havet/room/1~6/` |
| **수영장/스파/시설** | `/havet/special/1~10/` |
| **레스토랑** | `/havet/restaurant/`, `/havet/restaurant/food/`, `/havet/restaurant/gallery/` |
| **외부 전경** | `/havet/exterior/` |

이미지를 덮어쓸 경우 브라우저 캐시 때문에 바로 변경사항이 보이지 않을 수 있습니다. 이 경우 파일 이름 뒤에 쿼리 스트링을 붙이거나(예: `1.jpg?v=2`), 새 이름으로 업로드하는 것이 좋습니다.
