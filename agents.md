# AI 에이전트 작업 수칙 (Agent Guidelines)

본 프로젝트(하벳 리조트 홈페이지)에서 작업하는 모든 AI 에이전트는 아래의 6가지 핵심 원칙을 반드시 준수해야 합니다.

## 1. 최신 버전 기반 작업
- 작업을 시작하기 전, 반드시 현재 파일이 최신 버전인지 확인합니다.
- `git pull` 또는 `git status`를 통해 로컬 저장소의 상태를 점검한 후 작업을 진행합니다.

## 2. 요청 범위 엄수 (임의 수정 금지)
- 사용자가 명시적으로 요청하지 않은 작업은 **절대 수정하지 않습니다.**
- 특히 코드(HTML, CSS, JS) 수정 시, 지시받은 영역 외의 레이아웃, 디자인, 로직을 임의로 변경하거나 최적화하지 않습니다.

## 3. 작업 완료 후 히스토리 기록
- 작업이 완료되면 반드시 `docs/CHANGELOG.md` 파일에 변경 내역을 기록합니다.
- 어떤 파일을 어떻게 수정했는지 구체적으로 명시하여 작업 히스토리를 보존합니다.

## 4. 실수 및 이슈 기록
- 작업 중 실수를 발견하거나 예기치 않은 이슈가 발생할 경우, 즉시 관련 `.md` 파일(예: `docs/CHANGELOG.md` 또는 `docs/FEEDBACK.md`)에 해당 내용을 추가하여 기록으로 남깁니다.

## 5. 작업 전 문서(MD) 적용
- 코드를 수정하기 전에, 관련된 기획 문서나 작업 지시서(`.md`)를 먼저 작성하거나 업데이트합니다.
- 문서화가 선행된 후 실제 코드 작업에 들어갑니다.

## 6. 작업 전 본 수칙(agents.md) 준수
- 모든 새로운 세션이나 작업 시작 시, 본 `agents.md` 파일의 내용을 최우선으로 숙지하고 준수합니다.

---
*본 수칙은 하벳 리조트 홈페이지의 안정적인 장기 운영과 데이터 손실 방지를 위해 제정되었습니다.*
